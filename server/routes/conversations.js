const express = require('express');
const router = express.Router();
const conversationModule=require('../controllers/conversationCtrl');

//router.get('',conversationModule.getUser);

module.exports=router;